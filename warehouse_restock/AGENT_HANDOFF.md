# warehouse_restock — 赛狐海外仓备货单导入

EN BOM Cost List → 三成本拆分 → 赛狐海外仓备货单导入模板。

## 管道概要

```
EN BOM成本列表 → 成本借用(同重量模板) → 三成本拆分(绍兴/头程/加工)
    → 通途仓库映射(SKU出现在哪个仓) → 赛狐SKU白名单过滤
    → 填入海外仓备货单模板 → 输出
```

## 赛狐模板写入规则

| 模板列 | 公式 | 说明 |
|--------|------|------|
| `*收货仓库` | CENTRADE / DANEEY / POLAND | 按通途出现判断 |
| `*头程分摊方式` | 自定义 | 不启用赛狐自动分摊 |
| `*税费分摊方式` | 自定义 | |
| `*SKU` | 赛狐 SKU | |
| `指定采购单价` | 绍兴发货成本（单价） | |
| `*备货数量` | 1000 | 安全冗余 |
| `物流费用` | 头程运费 × 1000 | 总金额，不是单价 |
| `其他费用` | 国外加工成本 × 1000 | 总金额，不是单价 |
| `单个头程费用` | 留空 | 赛狐自动算: (物流+其他+税费)/数量 |
| `店铺` / `FNSKU` | 留空 | 共享库存 |
| 其余列 | 留空 | |

### 赛狐隐含分摊逻辑

赛狐自动计算 `单个头程费用 = (物流费用 + 其他费用 + 报关税费) / *备货数量`，
因此填物流费用/其他费用时需 × 备货数量。

未来如果确认赛狐订单里不能分开显示头程和加工成本，可简化为只填 `单个头程费用 = 头程运费 + 国外加工成本`，
物流费用/其他费用留空。

## 成本拆分逻辑

按 `绍兴发货方式` 分三条路径:

| 方式 | 绍兴发货成本 | 头程运费 | 国外加工成本 |
|:---:|---|---|---|
| 皮壳 | 皮壳成本 | 头程皮壳运费 | 对应仓库加工成本 |
| 半成品 | 皮壳成本 + 绍兴包装半成品成本 | 头程半成品运费(0时用皮壳) | 对应仓库加工成本 |
| 成品 | 绍兴总成本 | 头程成品运费 | 0 |

详见 `docs/bom_cost_explanation.md`

## 通途仓库映射规则

通途库存结存清单中，**只要 SKU 出现在某个仓库（哪怕库存=0）**，就认为该 SKU 在此仓有售。
不要求可用库存 > 0 —— 库存=0 意味着曾经有货、将来可能有货。

## 成本借用

同 `重量模板` 内的 SKU，同一成本列中 0/空值 被同组非 0 最大值替换。
借用范围：皮壳成本、绍兴包装成本、绍兴总成本、头程运费列、加工成本列。

## 函数索引

| 函数 | 职责 |
|------|------|
| `norm_col(name)` | 标准化 BOM 表头 (去空格、换行→`<br>`) |
| `build_col_map(df)` | 标准化表头 → 原始列名映射 |
| `resolve_warehouse(name)` | 仓库别名 → USNJ/USTX/PL |
| `split_cost_for_row(row, col_map, wh)` | 单行 SKU 在指定仓库的三成本拆分 |
| `borrow_costs(df, col)` | 同重量模板内成本借用 |
| `get_warehouse_sku_map(df_tongtu)` | 通途 → {SKU: [仓库列表]} |
| `fill_template(template_path, rows, out_path)` | 填入模板输出 |

## 赛狐导入规则（实测）

- **单个备货单 ≤ 500 条**（弹窗未注明，赛狐内部校验，超过全部失败）
- 总计 ≤ 5000 条
- 超过 500 条需拆批次（不同临时单号，脚本自动拆）
- 临时单号可选（单批导入时），多批需各自不同
- 同一备货单整体成功或失败，不同备货单互不影响
- 模板 2 行表头 + 7 sheet（4 hidden Data Validation）
- **去重**：同 (SKU, 仓库) 只保留一条（BOM 多客户物料号→同赛狐 SKU 导致重复）

## 踩坑

### 坑 1：500 条隐藏限制
赛狐弹窗未注明单个备货单上限，导入 852 条全部失败。错误信息"单个备货单商品数量不能超过500个"。

### 坑 2：去重后才能导入
同一赛狐 SKU 通过多个通途客户物料号映射到同仓，产生重复行。赛狐报"同一备货单下存在相同的SKU"。

### 坑 3：BOM 列名分隔符
BOM Excel 列名用 `, ` （逗号+空格）分隔，不是 `<br>`。脚本需用 `norm_col` 统一标准化。

