# 非技术团队 AI Agent 落地可行性报告

> 最后更新：2026-06-04  
> 回答：5 人+ 跨境电商团队，预算有限的场景下，如何让非技术同事用上 AI Agent

---

## 一、先毙掉一个选项：API 中转站

**别用。零容忍。**

搜到的真实数据：

| 风险 | 数据 |
|------|------|
| 恶意篡改命令（下载木马） | 428 个被测站中 **9 个** |
| 窃取 AWS 密钥等凭据 | **17 个** |
| 模型掺假（以次充好） | **45.83%** 的端点身份不匹配 |
| 花 Opus 的钱，实际调的是免费小模型 | 医疗问答准确率从 83.82% 暴跌至 37% |
| 链式嵌套（数据被多层读取） | 至少 **7 个中转站偷配了你的 Key** |
| 卷款跑路 | 预付费模式 + 上游封号 = 余额清零 |

> 你给 Agent 的是**本地终端执行权限**。一个恶意中转站可以在返回结果里注入 `rm -rf` 或窃取 `.env` 文件。这不是省钱，是开门揖盗。

---

## 二、成本方案：DeepSeek V4 团队共享

### 方案 A：共享 API Key（2-5 人，最简单）

DeepSeek 原生支持 `user_id` 参数做同账号多用户隔离：

```python
# 每个人用不同的 user_id，共享同一个 API Key
client.chat.completions.create(
    model="deepseek-chat",
    messages=[...],
    extra_body={"user_id": "zhangsan"}  # 每个人不同
)
```

| 限制 | 值 |
|------|-----|
| V4-Pro 并发 | 500（账号级，所有人共享） |
| V4-Flash 并发 | 2500 |

5 人以下团队通常够用。**成本**：API 按量付费，每人月均 ¥10-50。

### 方案 B：One-API 多 Key 负载均衡（5-20 人，推荐）

注册 3-5 个 DeepSeek 账号，用 [One-API](https://github.com/songquanpeng/one-api) 开源网关做负载均衡：

```
同事的 Agent → One-API 网关 → [Key1, Key2, Key3...] → DeepSeek
                                    ↓
                              429 自动故障转移
```

| 指标 | 单 Key | 5 Key 负载均衡 |
|------|--------|---------------|
| 429 错误/天 | 200+ | <3 |
| 可用率 | ~97% | 99.9%+ |

**成本**：One-API 免费开源，只需多注册几个 DeepSeek 账号（每个充值即可，无月费）。

---

## 三、当前 fzh-data 项目：Agent 能自举吗？

### 现状

你的项目已经有完整的 Agent 基础设施：

```
fzh-data/
├── AGENTS.md           ← 项目指令（Agent 自动读取）
├── .agents/skills/      ← 11 个技能（Agent 自动加载）
├── .codex/config.toml   ← Codex MCP 配置
└── docs/                ← 完整文档
```

### Agent 能否自动遵循？

**能，但有限制。** AGENTS.md 是行业标准（6 万+ GitHub 项目在用），Claude Code 和 Codex 都会自动读取。

量化效果（来自实际团队数据）：

| 指标 | 没用 AGENTS.md | 用了 AGENTS.md |
|------|---------------|---------------|
| 代码审查拒绝率 | 40% | 8%（↓80%） |
| 异常处理漏洞 | 12/月 | 2/月 |
| 硬编码密钥 | 5/月 | 0/月 |

**关键限制**：AGENTS.md 的规则是**概率性的，不是绝对强制执行**。Agent 会根据上下文权衡，不是你写什么它就一定照做。

### 对非技术同事的建议

告诉同事在 Agent 对话中说：

> "请阅读项目中的 AGENTS.md 和 .agents/skills/ 下的文件，按照项目规范执行"

Agent 会自动加载这些文件并按规则行事。

### 不同 Agent 的表现差异

| 场景 | Claude Code + DeepSeek | Codex + DeepSeek |
|------|----------------------|-------------------|
| 读取 AGENTS.md | ✅ 自动 | ✅ 自动 |
| 遵循 uv 管理环境 | ✅ | ✅（但需用户说） |
| 执行 Python 脚本 | ✅ | ✅ |
| Web 搜索/抓取 | ✅ WebFetch 内置 | ❌ 需装 MCP |
| 图片上传后 | 对话继续 | 线程卡死 |
| Token 效率 | 高（缓存命中好） | 低（同任务贵 10-20 倍） |

**结论：给同事推荐 Claude Code CLI + DeepSeek。** 不要让非技术同事用 Codex + DeepSeek（图片上传 = 对话死刑，他们搞不定）。

---

## 四、Hermes 服务器部署：能解决你担心的问题吗？

### 4.1 文件操作：服务器上怎么处理 Excel？

这是一个关键误解——**Hermes 服务器部署不等于文件只能通过网页上传**。

Hermes 有 6 种**终端后端**：

| 后端 | 文件访问方式 | 适用场景 |
|------|------------|---------|
| **Local**（桌面） | 直接读写本地文件系统 | 个人开发，跟 Claude Desktop 一样 |
| **Docker**（推荐生产） | 通过 volume 挂载目录 | 服务器上的工作目录 |
| **SSH** | 通过 SSH 操作远程机器文件 | 远程服务器/VPS |

**对你公司的实际含义**：

```
方案 A: 每个人在自己电脑上装 Claude Desktop/Codex
  → 文件在本地，Agent 直接读写（跟你现在一样）
  → git clone fzh-data，进入项目目录，Agent 自动读取 AGENTS.md

方案 B: 服务器上部署 Hermes + 每人用 Claude Desktop 连
  → 文件可以放在共享目录/NAS 上
  → Docker volume 挂载到容器
  → 非技术同事通过飞书/企微发消息给 Hermes
```

**方案 B 的问题**：如果 Excel 在同事的本地电脑上，不在服务器上，Hermes 碰不到——除非同事先用某种方式上传到共享目录。

**更实际的混合方案**：非技术同事装 Claude Desktop（3P 模式 + DeepSeek），git clone 你的项目，Agent 在**本地**操作文件。技术同事维护 Hermes 服务器做团队知识沉淀。

### 4.2 多人经验共享：成熟吗？

Hermes 的**自我进化双引擎**是这个问题的核心：

```
外驱引擎: 前台 Agent 执行 → 后台审阅集群分析 →
          识别重复模式 → 自动生成 Skill → 存储到共享库

内驱引擎: 结构化经验 → 可微分奖励信号 → LoRA 微调（规划中）
```

**Skills 自动生成流水线**：
1. **模式识别**：发现重复操作序列（比如"处理 Excel→清洗数据→生成报表"）
2. **流程抽象**：把具体参数替换为占位符，生成通用模板
3. **参数泛化**：建立约束模型，确保可复用

**实测效果**：持续使用 3 个月，处理效率提升 217%。社区积累 2000+ 可复用技能。

**多人共享机制**：
- Profiles 系统（v0.6.0+）：每个用户独立 config/memory/sessions/skills
- MEMORY.md（环境记忆）+ USER.md（用户画像）：跨会话持久化
- **联邦学习（v0.9.0 规划）**：多个实例共享经验数据

**诚实评估**：Hermes 的自我进化在**单用户**场景已有量化验证。**多人经验共享**目前更多是架构设计和规划，实测数据不够。如果这是你的核心需求，建议先观望，等 v0.9.0 联邦学习功能落地。

### 4.3 运行环境：每个人独立吗？

**是的。** Hermes Profiles 系统提供：

| 隔离维度 | 实现 |
|------|------|
| 配置 | 每个 Profile 独立 `config` |
| 记忆 | 独立 `MEMORY.md` + `USER.md` |
| 会话 | 独立 sessions 目录 |
| 技能 | 独立 skills 目录 |
| Python 环境 | Docker 模式用 `uv venv`（非 root，`gosu` 切到 hermes 用户） |

**uv/pandas 的处理**：Docker 镜像内 `uv venv` 创建独立虚拟环境。`pip install` 的包在容器内隔离，不污染宿主机。

---

## 五、务实推荐：分层方案

### 🥇 现在就能做的（成本低，风险低）

| 角色 | 工具 | 模型 | 月成本/人 |
|------|------|------|----------|
| **非技术同事** | Claude Desktop（3P 旧版）| DeepSeek V4 Flash | ¥10-30 |
| | git clone fzh-data | → Agent 自动读取 AGENTS.md + skills | |
| **技术同事** | Claude Code CLI | DeepSeek V4 Pro | ¥20-50 |
| **团队共享** | 1 个 DeepSeek API Key | 5 人共享 | ¥50-150 总计 |

不要让非技术同事用 Codex + DeepSeek。

### 🥈 3 个月后试点

- **Hermes 服务器**：Docker 双容器部署，飞书/企微接入
- 先让技术同事用，验证"自我进化"是否真的有效
- 验证通过后再推广给非技术同事

### 🥉 6 个月后评估

- Hermes v0.9.0 联邦学习是否落地
- DeepSeek V4.1 多模态是否发布（解决图片识别问题）
- 是否需要迁移到 Hermes 服务器方案

---

## 六、绝对不要做的事

| 不要 | 为什么 |
|------|--------|
| 用 API 中转站 | 6% 恶意率，45% 假模型，数据被多层读取 |
| 给非技术同事推 Codex + DeepSeek | 图片上传=对话死刑，搜索要装 MCP |
| 给非技术同事推新版 Claude Desktop 直接填 DeepSeek API | 5 月 6 日起已被白名单封杀 |
| 现在就全公司上 Hermes | 多人共享经验未充分验证 |
| 把 API Key 写在 IM 里发给同事 | 明文传输 = 等着泄露 |
