---
okf: v0.1
type: Handoff
title: 暂缓的三个 PR（#232 / #233 / #234）— 状态、卡点与重启条件
description: 三个 PR 因内容未验证或外部条件未定而暂缓并关闭。记录各自做到哪、卡在哪、什么条件下该回来重做、分支名，供后续会话直接接手。
date: 2026-09-21
agent: Claude Agent（PR 审批会话）
---

# HAND_OFF: 暂缓的三个 PR（#232 / #233 / #234）

三个 PR 于 2026-09-21 经决策**暂缓并关闭**：内容尚未验证（#232 未测试 / EN 未移植），或依赖的外部条件未落地（#233/#234 等 ChatGPT Business 试点结果）。不宜在此刻合入 main——会把未验证/可能过时的结论固化。

**PR 已关闭，分支全部保留**。重启时：从**最新 main** 开新分支（不要直接在旧分支上续，旧分支已和 main 有大量文档/索引冲突），按下面各节的「重启条件」重做或改造。

---

## #232 — 历史尾程费率表升级（暂缓：未测试 / EN 未移植）

- **分支**：`feature/history-last-leg-v2`
- **目标**：为 EN `History Last Leg Fee Record`（`HLF0001`，3,037 子行）升级做准备。
- **做了什么**：
  - 只读剖析（`history_last_leg_analysis.py`）：包裹去重、平台承担/FBA 排除、仓库/邮编/渠道标准化、克→千克换算、异常标记而非删除、稳健分层统计、覆盖率对账、`HLF0001` 忠实复刻回测。
  - **费用列口径更正**：建模与真值一律用 `物流商运费`（真实回传扣款）。`包裹总运费` 口径不稳（同一包裹时而等于物流商运费、时而等于通途运费），`通途运费` 是通途侧阶梯预估。**旧结论「按月份切换费用列」已作废**。
  - 多变体实测（`model_variants.py` + `compare_history_last_leg_variants.py`）：8 个变体 × 严格时间 Holdout + 两个真实月初工件（202605 / 202606）。
- **已知缺陷（背景）**：`HLF0001` 是全国均价 + 最近重量匹配，掩盖渠道与分区差异，且有克/千克量纲缺陷；线上另一处 `预估 × 发货数量` 而承运商按**整包**计费。
- **卡在哪**：用户「还没时间继续，有些复杂，就还没时间测试；EN 也没移植」。
- **重启条件**：① 对定稿模型做实测验证；② 把模型/脚本移植进 EN。之后才谈写回 `HLF0001` / Google Sheet。
- **注意**：全程只读，未写回 Google Sheet / 未改 `HLF0001` / 未做 ERPNext 写入——重启时也应先只读，确认口径后再动写。

---

## #233 — 深圳海外出口调研 +「当前配置来源优先级」（暂缓：外部结果未定）

- **分支**：`claude/condescending-cannon-b07539`
- **做了什么**（新增两份文档）：
  - `docs/research/2026-09-15-shenzhen-office-egress-and-chatgpt-business.md` — 复核 ChatGPT 分享对话三轮结论（含「深圳做 subnet router 就够」等不准确处的逐条核实）；封号风险实证（2023-04、2026-06 两次大规模封号、2026 三层风控机制）；关键结论「**共享代理出口是首要扣分项且一人违规全段连坐** → 统一出口在封号维度是负收益」「**ChatGPT 与 Codex 共用同一账号体系**，Codex 走近端 + 网页版走翻墙不能隔离风险」；方案矩阵 + 推荐路线 + 开放问题。
  - `docs/solutions/documentation-gaps/module-current-config-source-of-truth.md` — 可复用规则：如何判断模块「当前配置」。
- **卡在哪**：用户「刚开了 ChatGPT Business 账号，3 个席位，深圳有 1 个席位，可能多人用，不知道结果，可能个人用各自的翻墙也没啥影响」——真实使用结果未出来，调研结论可能随之修正。
- **重启条件**：ChatGPT Business 实际用起来、深圳席位使用方式（共用/各自）确定后，用真实结果复核并修正结论，再合。
- **教训（已落档）**：本次曾把 `.codex_tmp/` 下迁移前的快照当成北京办公室当前配置，被用户当场纠正——**判断「当前配置」要看现网/现库，别信临时快照**。

---

## #234 — AI 运营试点调研 bundle：Workspace Agent 能力边界（暂缓：与 #233 相关，结论待复核）

- **分支**：`claude/zen-torvalds-5b9c9d`
- **做了什么**：覆盖 `docs/ai-ops-pilot/` 整个 bundle；本 PR 新增 `workspace-agent-capability-boundary-2026-09.md`，回答 `brief-for-boss.md` R1 点名的五个工程问题并与仓库现有 Git 化方式逐项对比：
  1. 创建/编辑形态 —— 平台内表单 + 对话式生成，**无可导入/导出的定义文件** → 进不了版本控制
  2. 上传的是什么 —— Files（512 MB/文件、10 GB/agent）、Skill（**含 code**）；**上传代码是否执行官方未记载 → 未核实**
  3. 工具接入 —— 必须 MCP 且必须远程；私网走 Secure MCP Tunnel（出站单向）；Business **仅 Admin/Owner** 能开 developer mode 与发布 app
  4. 权限与治理 —— Agent 可原地更新 + 版本历史可回滚
  5. （见原文）
- **卡在哪**：用户「和 #233 相关，我在用 ChatGPT Business 进行中，之前的调研未必准确」——平台能力/约束还在变，早先的调研结论可能过时。
- **重启条件**：ChatGPT Business 用法稳定、平台能力边界清楚后，逐条复核「未核实/可能不准」处，再合。

---

## 共同提醒

- **不要直接在旧分支续做**：这三个分支已与 main 有大量共享文档/索引冲突（每合一批 PR 就冲突一次）。重启时从最新 main 开新分支，把需要的内容挑过去。
- **合前过惯例门槛**：四组凭证扫描零输出 + `git diff --check` 干净 + 测试通过 + 索引/OKF 级联更新。
- **本文件自身**：只是一份状态交接，不含未验证结论；重启完成后可删除或更新。
