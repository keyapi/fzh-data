---
okf: v0.1
type: Spec
title: Portal PoC E2E 自测报告
description: nginx /chat /ops 门户融合本地验收；运营审 DEFERRED
tags: [portal, e2e, nginx, open-webui, verification]
timestamp: 2026-07-24
---

# Portal PoC — E2E 自测报告

**日期**: 2026-07-24  
**环境**: Windows + Docker Desktop；壳 OWUI 已在 `:3000` 健康运行  
**入口**: `http://127.0.0.1:8088/`  
**运营审**: **DEFERRED**（本报告不依赖运营签字）

## 启动命令

```powershell
cd ai_access_poc/open_webui
docker compose up -d

cd ../portal
Copy-Item .env.example .env -ErrorAction SilentlyContinue
docker compose up -d --build
uv run python scripts/e2e_verify.py
```

## 检查结果（`scripts/e2e_verify.py`）

机器可读快照：[`_e2e_last.json`](_e2e_last.json)

| Check | 结果 | 说明 |
|-------|------|------|
| portal_health | **PASS** | `GET /health` → `fzh-ai-portal` |
| landing | **PASS** | `GET /` 含 FZH AI Portal |
| chat_redirect | **PASS** | `GET /chat` → `Location: /chat/`（相对路径，保留 :8088） |
| chat_proxy | **PASS** | `GET /chat/` → OWUI HTML（~11KB） |
| chat_root_assets | **PASS** | `GET /api/config` → 200（根路径劫持） |
| ops_page | **PASS** | `GET /ops/` Board PoC stub |
| ops_health | **PASS** | candidates.available=true；钉钉 dry-run |
| ops_candidates | **PASS** | 19 条候选（真实 `board/out`） |
| auth_status | **PASS** | `mode=dry-run` |
| oidc_path | **PASS** | profile 未启 → 503 dry-run JSON |

**汇总**: 10/10 PASS

## 浏览器抽检

| URL | 观察 | 截图（本地，未入 git） |
|-----|------|------------------------|
| `http://127.0.0.1:8088/` | 落地页 + Chat/Ops 按钮 | `portal/screenshots/portal-landing.png`（gitignore） |
| `http://127.0.0.1:8088/ops/` | 店铺 TOODDLY-Daneey-US、候选 19、钉钉 dry-run、运营审 DEFERRED | `portal/screenshots/portal-ops.png` |
| `http://127.0.0.1:8088/chat/` | Open WebUI 登录页「FZH AI Shell PoC」 | `portal/screenshots/portal-chat.png` |

> 公开仓禁止提交图片（CONTRIBUTING / AGENTS.md）；截图仅本地留存作验收证据。

## 路由实现摘要

| 路径 | 行为 |
|------|------|
| `/chat/` | strip 前缀 → `open-webui:8080`（外部网络 `open_webui_public`） |
| `/_app/` `/static/` `/api/` `/ws` … | 同劫持到 OWUI（官方不支持真正子路径） |
| `/ops/` | `ops-stub:8090`，读 `board/out` 或 `fixtures/board_out` |
| `/oidc/` | profile `dingtalk`；未启 → dry-run |

## Blockers / 已知限制

| 项 | 状态 |
|----|------|
| 钉钉 AppKey/Secret | 未配置 → dry-run 路径已交付；启用见 `docs/reference/dingtalk-oidc.md` |
| IvyeaOps 完整 SPA | 仓外 fork 无预构建 `client/dist`；本 PoC 用 stub + 真实 candidates；可选 `IVYEAOPS_UPSTREAM` |
| OWUI 子路径 | 官方不支持；资产劫持为 PoC 方案；生产更推荐子域 |
| 运营审 | **DEFERRED** — 不阻塞本 Portal 切片 |
| PR #117 docs closeout | 仍 OPEN；本 Portal 分支基于 `origin/main`，互不阻塞 |

## 一键复现（明天）

```powershell
cd d:\Work\赛狐\Cursor\ai_access_poc\portal
powershell -ExecutionPolicy Bypass -File scripts\start_portal.ps1
# → http://127.0.0.1:8088/
```
