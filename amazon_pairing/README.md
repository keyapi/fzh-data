# amazon_pairing — Amazon 在售未配对自动匹配建议

赛狐 Amazon 在线商品配对辅助子项目：只读盘点、别名匹配、规则/ML 候选建议，最终给运营生成可核对/可导入表格。

新对话先读 [AGENT_HANDOFF.md](AGENT_HANDOFF.md)。当前依赖 `missing_products/` 的缓存、映射表和通途别名读取脚本，不单独拉生产数据写入赛狐。
