# fzh-web-automation 环境变量模板
# 复制为 .env 后填写实际值（.env 已被 .gitignore 排除）

# --- 通途 ERP ---
# 自动登录凭据（首次运行或 cookie 过期时使用 ddddocr 识别验证码登录）
TONGTU_USER=你的通途账号邮箱
TONGTU_PASSWORD=你的通途密码

# 账号密码通过浏览器手动登录一次后，cookie 自动持久化到 chrome-profile/
# 之后无需再配置

# --- 赛狐 ERP ---
# 账号密码通过浏览器手动登录一次后，cookie 自动持久化到 sellfox-profile/
# 无需在此配置

# --- 钉钉（aflow / OA审批管理后台）---
# 账号密码登录（推荐，免扫码）。留空则回退到「一键头像授权 / 扫码」人工登录。
# 流程：账号登录 tab → 手机号 → 下一步 → 密码 → 登录。
# 密码步没有图形验证码；陌生设备/新 profile 之后可能还要一次短信验证码（必须人工，同 profile 之后不再要）。
# 登录态持久化到 dingtalk-profile/。
DINGTALK_USER=你的钉钉手机号
DINGTALK_PASSWORD=你的钉钉密码
# 登录后要选的组织全称（不要把真实组织名写进 git 脚本默认值）
DINGTALK_ORG=你的钉钉组织全称
# 导出/附件落盘目录。与 dingtalk_oa_approval 共用；不要用含人名的本机路径。
# 也可写在 dingtalk/dingtalk_oa_approval/.env 的 DINGTALK_OA_WORK。
DINGTALK_OA_WORK=

# --- Playwright ---
# 浏览器默认使用 persistent_context，无需额外配置
# MCP 模式使用独立浏览器实例
