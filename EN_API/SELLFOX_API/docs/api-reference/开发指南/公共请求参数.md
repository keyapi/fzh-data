# 公共请求参数

### 请求域名：
https://openapi.sellfox.com
### 请求路径：
参见各个接口请求路径
### 公共请求参数
公告请求参数除过去授权外，所有接口均需传入，拼接在请求域名和请求路径后，详见各个接口公共请求参数
### 调用说明
1. 除了获取token接口其余所有请求均为post，所有接口请求**requestBody均为json**；
2. 除了获取token接口其余所有请求均需在header中传入 **Content-Type: application/json**

