---
okf: v0.1
type: Research
title: 蜴国际 P0 样例列映射（2026-07-17）
description: 对蜥蜴国际-p0-样例目录四文件的结构分析：匹配键、重量单位、批次关系与 P1B 导出/导入要点
timestamp: 2026-07-17
tags: [sellfox-shipping, lizard, p0, excel, column-mapping]
---

# 蜴国际 P0 样例列映射（2026-07-17）

> 原始文件在 `sellfox_shipping/数据源/蜥蜴国际-p0-样例/`（gitignore，含 PII，不入 Git）。  
> 业务说明：① 与 ②–④ **不是同一操作批次**；当前生产是「通途 Excel → 小程序变格式 → 上传蜴国际」，尚未用赛狐导出 Excel 走该流程。

## 1. 文件清单与来源

| # | 实际文件（前缀 + 原名） | 来源 | 结论 |
|---|------------------------|------|------|
| ① | `01-sellfox-export-by-package 赛狐按包裹导出2026_07_16(1).xlsx` | 同事 A | **不可用**：仅 1 行表头 `包裹号`，无数据行 |
| ② | `02-lizard-upload 用于上传到蜴国际的excel.xls` | 同事 B | 上传模板；`.xls`；38 行包裹 |
| ③ | `03-lizard-tracking-return 蜴国际下载的追踪号的 excel.xlsx` | 同事 B | 返回追踪号；99 行；含 ② 全部参考号 |
| ④ | `4 04-lizard-labels-2026-07-15 7.15蜴国际面单.pdf` | 同事 B | ~306 KB 面单合订；P1B 可不解析 |

**批次关系（同事 B）：** ②∩③ = 38；②⊂③。③ 另有 61 单不在本次上传样例中（同日更大返回集）。  
**与本地 `shipping_packages`（近几日 API sync）：** ② 的 38 个 `P8140…` **零命中**——样例偏旧，勿用当前库做联调对账。

## 2. 匹配键（已验证）

| 角色 | 列名 | 样例值 | 用途 |
|------|------|--------|------|
| **主键** | `参考编号/Reference Code` | 样例 `P81401351`（**通途**包裹号）；赛狐应为 `P2A…` | 上传与返回 **原样一致**。业务确认：未来填赛狐 `packageSn`，不要填通途号 |
| 追踪号 | `物流单号`（仅 ③） | `382619183572` | FedEx 风格数字；③ 中无空值 |
| 蜴国际订单号 | `订单号`（仅 ③） | `M6180202607158934628` | 物流商侧单号；**不是**赛狐 `orderId` |
| 备注 | `备注/Remark` | `TT0000…` / `CEN…` | 商品/通途侧标识，**勿作匹配键** |

**P1B 规则：** 只按 `参考编号/Reference Code` ↔ 本地 `package_sn` 对账；禁止按行序/页序。

## 3. ② 上传表结构

- 工作表：`自定义邮寄方式数据`
- 行数：表头 + 38
- 列（22）：

`参考编号/Reference Code` · `派送方式/Delivery Style` · `渠道优选` · `全名/Consignee Name` · `收件人国家/Consignee Country` · `州/Province` · `城市/City` · `地址1/Street1` · `地址2/Street2` · `邮编/Zip Code` · `收件人电话/Consignee Phone` · `备注/Remark` · `重量` · `长` · `宽` · `高` · `箱数` · `收件人公司名称` · `发货编码/shipper Code` · `签名服务` · `计量单位` · `门牌号`

样例观察：

| 字段 | 观察 |
|------|------|
| 派送方式 | 上传时常为空；返回后由蜴国际填渠道名 |
| 渠道优选 | 样例均为「是」 |
| 国家 | 英文全称 `United States`（返回多为 `US`） |
| 电话 | 占位 `0000000000` |
| 发货编码 | `S0143` |
| 计量单位 | `cm/kg` |
| **重量** | **克级整数**（如 `3390`），与返回 kg **不一致** |

## 4. ③ 返回表结构（对账相关列）

工作表 `Sheet1`；表头约 76 列（大量费用列可先忽略）。对账最小集：

| 列 | 样例 |
|----|------|
| `参考编号/Reference Code` | `P81400983` |
| `派送方式/Delivery Style` | `FedEx-Economy-10-HOU` / `FedEx-Eco-21-TX` / … |
| `物流单号` | 追踪号 |
| `订单号` | 蜴国际 `M6180…` |
| `订单状态` | 样例均为 `已预报` |
| `重量(kg)` / `长(cm)` / `宽(cm)` / `高(cm)` | 3.485 / 48 / 42.2 / 15 |
| `运费` / `包裹总金额` | 费用；有则写入预估/最终成本 |

派送方式枚举（本样例）：`FedEx-Economy-10-HOU`、`FedEx-Eco-21-TX`、`FedEx-Economy-10-USEA`、`FedEx-Ground-J-TX`。

## 5. 单位陷阱（实现必守）

| | ② 上传 | ③ 返回 |
|--|--------|--------|
| 重量列名 | `重量` | `重量(kg)` |
| 数值 | `3390`（克） | `3.485`（千克） |
| 尺寸 | `长/宽/高` 无单位后缀 | `长(cm)/宽(cm)/高(cm)` |

导出到蜴国际时须与现网小程序一致（克 + cm）；导入时按返回列单位解析，**不要**把 3390 当 kg。

## 6. 对 P0 / P1B 的状态

| 项 | 状态 |
|----|------|
| ② 上传列结构 | 已记录，可开始 `SpreadsheetCarrierAdapter` 导出骨架 |
| ③ 返回匹配键 + 追踪号列 | 已记录，可开始导入/对账骨架 |
| ④ PDF | 已收集；P1B 可后置 |
| ① 赛狐导出对照 | **未完成** — 需同事 A 重新按包裹导出含数据行的文件 |
| 业务方签字确认映射 | **待你确认** 本节匹配键与单位规则 |
| `submitToPlatform` 合约 | 仍未验证（与本样例无关） |

## 7. 已确认 / 仍待补齐

1. **已确认：** 参考编号 = 赛狐 `packageSn`（`P2A…`；P0 样例 `P8140…` 仅反映通途旧路径）。  
2. **重量：** 不纠结「是否永远用克」——按蜴国际上传模板要求换算即可；同事 B 样例数值为克级 + `计量单位=cm/kg`。  
3. **缺口（已修正）：** 包裹 API 无重尺；应用 `commoditySku` → `/api/commodity/pageList.json`（`isGroup=0`）取 `cartonWeight`×1000 与 LWH。详见 [sellfox-carton-dims-source-2026-07-17.md](sellfox-carton-dims-source-2026-07-17.md)。试转换已重生成：**8/10 有重尺**，2 单 `KS0002-DL-194-*` carton 为 0 待 ERPNext 兜底。  
4. **发货编码：** 暂固定 `S0143`；日后或按仓（USNJ/USTX）分号结算。  
5. ① 赛狐导出仍建议重导有数据行；notebook 遗产见 [colab-notebook-legacy-summary-2026-07-17.md](colab-notebook-legacy-summary-2026-07-17.md)。
