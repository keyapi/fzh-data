# SPS Commerce Selenium 自动化依赖（sps_automation.py / run_simple.bat）
# 本目录同时存在赛狐网页登录脚本（sellfox_login.py 等），其 ddddocr/onnxruntime
# 依赖已随子项目迁移到 web_automation（PR #208），不再放这里。
selenium==4.34.2
typing_extensions==4.14.1
trio==0.30.0
trio-websocket==0.12.2
websocket-client==1.8.0
urllib3==2.5.0
outcome==1.3.0
sniffio==1.3.1
attrs==23.2.0
wsproto==1.2.0
certifi>=2025.6.15

# ChromeDriver 自动管理（sps_automation.py 与 sellfox 登录共用）
webdriver-manager>=4.0.0

# Logging / config（config.py 从 .env 读取凭据）
python-dotenv==1.0.0

# Retry
tenacity==8.2.3

# zoneinfo is stdlib on Python 3.9+
